// ------------------------------------------------------------------
// RENAME THIS FILE TO env.js AND FILL IN YOUR ACTUAL KEYS
// ------------------------------------------------------------------

// Your web app's Firebase configuration
const firebaseConfig = {
  // REPLACE with your Firebase project's apiKey
  apiKey: "YOUR_FIREBASE_API_KEY",
  // REPLACE with your Firebase project's authDomain
  authDomain: "YOUR_FIREBASE_AUTH_DOMAIN",
  // REPLACE with your Firebase project's projectId
  projectId: "YOUR_FIREBASE_PROJECT_ID",
  // REPLACE with your Firebase project's storageBucket
  storageBucket: "YOUR_FIREBASE_STORAGE_BUCKET",
  // REPLACE with your Firebase project's messagingSenderId
  messagingSenderId: "YOUR_FIREBASE_MESSAGING_SENDER_ID",
  // REPLACE with your Firebase project's appId
  appId: "YOUR_FIREBASE_APP_ID",
};

// Your reCAPTCHA v3 site key
// REPLACE with your reCAPTCHA site key
const RECAPTCHA_SITE_KEY = "YOUR_RECAPTCHA_SITE_KEY";
